export interface ConfigObject {
  graphqlUrl: string
  token: string
  collectionGid: string
}

const config: ConfigObject = {
  graphqlUrl: 'Your graphql url',
  token: 'api token',
  collectionGid: 'your product collection id',
}

export default config
